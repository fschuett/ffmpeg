diff -pruN csrc.orig/libavcodec/avcodec.h csrc/libavcodec/avcodec.h
--- csrc.orig/libavcodec/avcodec.h	2018-10-03 08:37:59.060749570 +0200
+++ csrc/libavcodec/avcodec.h	2018-10-03 10:35:38.988215091 +0200
@@ -1767,7 +1767,7 @@ typedef struct AVCodecContext {
      * - encoding: unused
      * - decoding: Set by user, if not set the native format will be chosen.
      */
-    enum AVPixelFormat (*get_format)(struct AVCodecContext *s, const enum AVPixelFormat * fmt);
+    long get_format;
 
     /**
      * maximum number of B-frames between non-B-frames
@@ -6032,7 +6032,7 @@ enum AVLockOp {
  *             build with thread support to get basic thread safety.
  */
 
-int av_lockmgr_register(int (*cb)(void **mutex, enum AVLockOp op));
+//int av_lockmgr_register(int (*cb)(void **mutex, enum AVLockOp op));
 #endif
 
 /**
