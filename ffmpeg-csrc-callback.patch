diff -pruN csrc.orig/libavcodec/avcodec.h csrc/libavcodec/avcodec.h
--- csrc.orig/libavcodec/avcodec.h	2018-04-19 21:24:36.000000000 +0200
+++ csrc/libavcodec/avcodec.h	2018-04-20 14:21:17.172847634 +0200
@@ -2035,7 +2035,7 @@ typedef struct AVCodecContext {
      * - encoding: unused
      * - decoding: Set by user, if not set the native format will be chosen.
      */
-    enum AVPixelFormat (*get_format)(struct AVCodecContext *s, const enum AVPixelFormat * fmt);
+    long get_format;
 
     /**
      * maximum number of B-frames between non-B-frames
@@ -6282,7 +6282,7 @@ enum AVLockOp {
  *           implement your lock manager). If cb is set to NULL the
  *           lockmgr will be unregistered.
  */
-int av_lockmgr_register(int (*cb)(void **mutex, enum AVLockOp op));
+//int av_lockmgr_register(int (*cb)(void **mutex, enum AVLockOp op));
 
 /**
  * Get the type of the given codec.
